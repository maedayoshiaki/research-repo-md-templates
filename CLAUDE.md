# CLAUDE.md

このリポジトリの運用規約は `AGENTS.md` に集約しています。**作業前に必ず読んでください。**

下の行で AGENTS.md を取り込みます（Claude Code は `@パス` で他ファイルを読み込みます）。

@AGENTS.md

## Claude Code Notes（任意・固有メモだけ）

- 規約の追加・変更は **`AGENTS.md` 側**に書く（このファイルは参照役に徹し、規約を二重管理しない）
- {{Claude Code だけで使うショートカットや注意があればここに。例: よく使うスラッシュコマンド}}
- 別案: このファイルを置かず `ln -s AGENTS.md CLAUDE.md` のシンボリックリンクで同一ファイルを読ませる方法もある（Windows や固有メモが必要な場合は現行の import 方式を推奨）
